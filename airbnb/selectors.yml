selectors:
  - name: dim_except_listings_w_hosts
    description: Select all dim models, excluding dim_listings_w_hosts
    definition:
      union:
        - method: path
          value: models/dim
        - exclude:
            # fqn: Full Qualified Name
            - method: fqn
              value: dim_listings_w_hosts